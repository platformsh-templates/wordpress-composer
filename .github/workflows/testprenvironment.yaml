name: "TestPrEnvironment"
on:
  workflow_run:
    workflows: [ "platformsh" ]
    types:
      - completed
  pull_request:
    branches:
      - master
      - main
env:
  # @todo I dont think this is needed anymore
  GITHUB_TOKEN: ${{ secrets.TEMPLATES_GITHUB_TOKEN }}
jobs:
  call-reusable-testpr:
    uses: platformsh/ghrw-templates/.github/workflows/testprenvironment.yaml@9bff6d6cb7039625a5cf44004ee0eccd39b99d54
    secrets: inherit

